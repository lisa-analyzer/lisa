class factorialInterleaved {

	factorial(n) {
		if (n <= 1) 
			return 1;
		else {
			def x = n - 1;
			this.interleaved(x);
			return this.factorial(x) * n;
		}
	}
	
	interleaved(x) {
		return x + 1;
	}
	
	main(a) {
		def x = this.factorial(a);
    }
}