class tests {

	loop(n) {
		if (n < 0)
			return 10;
		else {
			def x = n - 1;
			return this.loopAux1(x) + n;
		}
	}
	
	loopAux1(n) {
		def x = n - 2;
		return this.loopAux2(x) + n;
	}
	
	loopAux2(n) {
		def x = n + 1;
		return this.loop(x) + n;
	}
	
	main(a) {
		def z = this.loop(a);
    }
}